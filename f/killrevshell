#!/bin/bash
killall sshd
sshd -p 8022

REVERSE_SHELL_PORT=$(cat ./DEFAULT_PORT)
REMOTE_HOSTNAME=$(cat ./REMOTE_HOSTNAME)

pkill -f "ssh -R ${REVERSE_SHELL_PORT}:127.0.0.1:8022 ${REMOTE_HOSTNAME} -fNT"
